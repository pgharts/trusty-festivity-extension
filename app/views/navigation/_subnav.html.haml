%section{class: "subnav #{subnav_class}"}
  - current_page.children.each_with_index do |page, index|
    - if page.hide_from_subnav != true
      = render partial: 'navigation/subnav_item', locals: {page: page}